#include "main.h"
#include <string.h>

/**
 * _strlen - This function dispalys the length of
 * a string
 * @s: parameter given
 * Return: length;
 */

int _strlen(char *s)
{
	int len;

	len = strlen(s);
	return (len);
}
