#include <stdlib.h>

/**
 * Description: _abs - display the absolute value of an interger
 * @num: parameter
 * Return: 0 (success)
 */

int _abs(int num)
{
	int n;

	if (num < 0)
	{
		n = -1 * num;
		return (n);
	}
		return (num);

}
